/// Common European Framework level. `null` on [Unit.level] for Tier 0
/// content (Antiguan Creole), which is organized by topic instead — the
/// CEFR framework does not map meaningfully onto it (CONTENT-AUTHORING.md).
enum Cefr { a1, a2, b1, b2, c1 }
