/// The task categories PROVIDERS.md's routing table is keyed by. Routing
/// is per task, not global.
enum LlmTask {
  conversationPractice,
  grammarExplanation,
  sentenceGeneration,
  errorCorrection,
  translationCheck,
  offline,
}
